# Vissari
🗺️ Species Distribution Modeling (SDM)
Проект: Моделирование и прогнозирование ареалов обитания биологических видов
## Описание проекта
Данный проект реализует полный пайплайн (конвейер) для прогнозирования потенциального ареала обитания биологических видов на основе машинного обучения и геоинформационных систем (ГИС).

Цель: Создание инструмента, который автоматически собирает данные о встречах вида и климатических условиях, чтобы построить карту вероятного распространения вида (ареал) в заданном регионе (Европа).

Ключевые особенности и техническая сложность:
 * Автоматизированный сбор данных о присутствии вида через iNaturalist API.
 * Работа с многослойными растровыми геоданными (19 биоклиматических переменных WorldClim).
 * Применение алгоритма Random Forest Classifier для моделирования экологической ниши.
 * Реализация Пространственной кросс-валидации (Spatial Cross-Validation) для строгого тестирования надежности модели.
   
2. Структура проекта
   

├── areal_from_weather.py        # Главный скрипт, содержащий всю логику (сбор данных, ML, валидация, визуализация)

├── README.md                     # Этот файл

├── weather/                       # Папка для климатических данных WorldClim (требуется вручную скачать)

│   ├── wc2.1_10m_bio_1.tif

│   └── ... (wc2.1_10m_bio_19.tif)

## Рекомендуется использовать виртуальное окружение

Для запуска проекта необходимо установить следующие библиотеки Python.

pip install pandas geopandas rasterio shapely numpy matplotlib scikit-learn requests

#### Загрузка климатических данных (КРИТИЧЕСКИ ВАЖНО)
Проект использует 19 биоклиматических переменных WorldClim. Эти файлы не включены в репозиторий из-за большого объема.
 * Скачайте архив Current climate (Bioclim) с разрешением 10 minutes (или 5m) с сайта WorldClim - BioClim.
 * Создайте в корне проекта папку с названием weather (как указано в коде areal_from_weather.py).
 * Распакуйте все 19 файлов .tif (от wc2.1_10m_bio_1.tif до wc2.1_10m_bio_19.tif) в эту папку.
## Настройка и запуск
Инструкция по Использованию (МАНУАЛ)
Скрипт разработан для интерактивного запуска через командную строку.
1. Запуск Скрипта
Откройте терминал или командную строку, перейдите в папку с проектом и запустите скрипт:
python areal_from_weather.py

2. Ввод ID Вида (Taxon ID)
При первом запуске скрипт запросит ID вида (taxon_id) с iNaturalist, для которого будет строиться модель.
> введите ID вида:
> 
Пример: Введите 10070 (для Зяблика _Fringilla coelebs_).
Скрипт автоматически выполнит полный цикл работ:
 * Сбор данных: Загрузка всех наблюдений за вид с 2014 года по текущий месяц.
 * Обработка: Очистка, геопространственная фильтрация (только Европа, как задано в EUROPE_BBOX), удаление дубликатов по растровой сетке.
 * Создание выборок: Генерация псевдо-точек "Отсутствия" (в 2 раза больше, чем точек "Присутствия").
 * Связывание с климатом: Извлечение 19 биоклиматических переменных WorldClim для всех точек. Файл сохраняется как species_with_bioclim.csv.
 * Обучение и анализ: Обучение модели Random Forest, вывод метрик и отображение графика важности признаков.
 * Генерация карты: Построение и сохранение GeoTiff карты вероятности обитания для всей Европы (europe_distribution.tif).
3. Интерактивное Меню (После построения модели)
После завершения первого цикла появится меню для дальнейших действий:
* Для создания новой модели введите 1  
* Для валидации модели введите 2 
* Для выхода введите 0 

| Опция | Действие | Назначение |
|---|---|---|
| 1 | Создать новую модель | Начнет процесс заново, позволяя ввести новый ID вида или перестроить модель для существующего. |
| 2 | Валидация модели | Запускает критически важный этап проверки адекватности. Сравнивает результаты обычной (случайной) кросс-валидации с пространственной (блочной) кросс-валидацией (calculate_spatial_fold_accuracy). |
| 0 | Выход | Завершает работу скрипта. |

Важность Пространственной Валидации (Опция 2)

* Если модель хорошо предсказывает ареал только в тех же географических блоках, где она обучалась, это указывает на переобучение (overfitting).
* Если Средний ROC-AUC при пространственной валидации близок к ROC-AUC при обычной валидации, модель считается АДЕКВАТНОЙ и способной экстраполировать предсказания на новые территории.
* Если наблюдается сильное падение точности, вы получите предупреждение о возможном переобучении.
  
## Основные Функции Скрипта
| Функция | Описание |
|---|---|
| get_data(taxon_id) | Загружает наблюдения (Research Grade) с iNaturalist по месяцам и сохраняет их в CSV (<taxon_id>_all.csv). |
| clean_data() | Читает CSV, парсит координаты, фильтрует по Европе (EUROPE_BBOX) и удаляет дубликаты точек, находящиеся в одной ячейке растра. |
| data_absens(gdf_clean) | Генерирует псевдо-точки "отсутствия" (в два раза больше, чем точек присутствия) в пределах Европы, избегая ячеек с "nodata" значениями. |
| weather_data() | Объединяет очищенные данные присутствия/отсутствия с 19 биоклиматическими переменными WorldClim. |
| model() | Обучает модель RandomForestClassifier, выводит отчет о классификации, ROC-AUC и строит график важности признаков. |
| generate_prediction_map(...) | Использует обученную модель для предсказания вероятности обитания в каждой ячейке растра в пределах Европы. Сохраняет результат в GeoTiff. |
| calculate_spatial_fold_accuracy(...) | Реализует пространственную кросс-валидацию, деля данные на географические блоки (по умолчанию 5x5 градусов). |
| validation(...) | Сравнивает обычную и пространственную валидацию для оценки адекватности и способности модели к экстраполяции. |
## Важные Замечания
 * Настройки пути: Убедитесь, что переменная PATH_TO_WORLDCLIM = "./weather" указывает на правильную директорию с TIF-файлами WorldClim.
 * Границы BBOX: Границы региона для моделирования заданы в EUROPE_BBOX = (-25.0, 34.0, 45.0, 72.0) (Запад, Юг, Восток, Север). Измените их, если требуется другой регион.
 * iNaturalist API: Скрипт может работать медленно при загрузке данных за много лет, так как последовательно запрашивает данные по месяцам, чтобы избежать ограничений API.

