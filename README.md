# Vissari
🗺️ Species Distribution Modeling (SDM)
Проект: Моделирование и прогнозирование ареалов обитания биологических видов
1. Описание проекта
Данный проект реализует полный пайплайн (конвейер) для прогнозирования потенциального ареала обитания биологических видов на основе машинного обучения и геоинформационных систем (ГИС).
Цель: Создание инструмента, который автоматически собирает данные о встречах вида и климатических условиях, чтобы построить карту вероятного распространения вида (ареал) в заданном регионе (Европа).
Ключевые особенности и техническая сложность:
 * Автоматизированный сбор данных о присутствии вида через iNaturalist API.
 * Работа с многослойными растровыми геоданными (19 биоклиматических переменных WorldClim).
 * Применение алгоритма Random Forest Classifier для моделирования экологической ниши.
 * Реализация Пространственной кросс-валидации (Spatial Cross-Validation) для строгого тестирования надежности модели.
2. Структура проекта
.
├── areal_from_weather.py     # Главный скрипт, содержащий всю логику (сбор данных, ML, валидация, визуализация)
├── README.md                 # Этот файл
├── weather/                   # Папка для климатических данных WorldClim (требуется вручную скачать)
│   ├── wc2.1_10m_bio_1.tif
│   └── ... (wc2.1_10m_bio_19.tif)
└── output/                   # Автоматически создается для сохранения результатов (карт, графиков)

3. Требования к окружению и установка
Для запуска проекта необходимо установить следующие библиотеки Python.
3.1. Установка зависимостей
# Рекомендуется использовать виртуальное окружение
pip install pandas geopandas rasterio shapely numpy matplotlib scikit-learn requests

3.2. Загрузка климатических данных (КРИТИЧЕСКИ ВАЖНО)
Проект использует 19 биоклиматических переменных WorldClim. Эти файлы не включены в репозиторий из-за большого объема.
 * Скачайте архив Current climate (Bioclim) с разрешением 10 minutes (или 5m) с сайта WorldClim - BioClim.
 * Создайте в корне проекта папку с названием wether (как указано в коде areal_from_weather.py).
 * Распакуйте все 19 файлов .tif (от wc2.1_10m_bio_1.tif до wc2.1_10m_bio_19.tif) в эту папку.
4. Настройка и запуск
 * Настройка ID вида: Откройте файл areal_from_weather.py. Найдите и измените переменную taxon_id (ID вида на iNaturalist) на тот ID, который вы хотите исследовать.
   # Пример настройки в areal_from_weather.py
TAXON_ID = 10000 # ID вашего вида (например, ID Снегиря или Славки)

 * Запуск скрипта:
   python areal_from_weather.py

5. Результаты работы
После завершения работы все выходные файлы будут сохранены в папке output/.
 * Карта ареала: distribution_map.png (визуализация) и distribution_map.tif (растровый файл для ГИС).
 * Графики:
   * feature_importance.png: Диаграмма важности биоклиматических факторов (показывает, какой климат наиболее критичен для вида).
   * validation_comparison.png: Сравнение результатов Обычной и Пространственной валидации (ключевой слайд для защиты).
 * Консольный вывод: Отчет по метрикам ROC-AUC.
